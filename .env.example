# Copy to .env.local for local debug runs. Dart defines take precedence.
# Public client configuration only. Never add backend secrets.
API_BASE_URL=
CLERK_API_ENDPOINT=
CLERK_PUBLISHABLE_KEY=
# stage: 100% traces; production: 50%. Independent of build mode.
APP_ENV=stage
SENTRY_DSN=
